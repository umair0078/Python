# print => use to print string in quotient marks.
# sqrt() => returns the square root of the number => inside math module
# pow() => returns the power of a number

#These library functions are defined inside the module. And, to use them we must import the module inside our program.

import math

square_root = math.sqrt(9)
print(square_root)


power = pow(2,3)

print("2 to the power 3 is", power)